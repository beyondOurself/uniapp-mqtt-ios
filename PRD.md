# 产品需求文档（PRD）

_版本：v1.0_  
_更新日期：2026-09-24_

> 验证 uni-app 运行到 iOS 时的 MQTT 连接。范围仅联调样例，不做业务产品。

## 一、项目背景及目标

提供最小 uni-app App，在 iOS 真机上验证 MQTT 能否连接、订阅、收发消息，并沉淀版本与兼容处理说明。

## 二、目标用户

- 需要排查 / 移植 uni-app iOS MQTT 的研发同学

## 三、需求范围及版本规划

### MVP（当前）

- 选择预发 / 生产 Broker
- 连接 / 断开 MQTT
- 订阅主题、发布消息
- 页面内滚动日志（含平台与 Socket 能力探测）

### 非目标

- 登录、推送、RTC、业务信令协议
- 升 mqtt 到 4.x、多端产品化 UI

## 四、详细功能列表

| 功能 | 说明 | 验收 |
|------|------|------|
| 环境切换 | 预发 / 生产卡片 | 未连接时可切换 |
| 连接 | `mqtt.connect` + 账号 | iOS 真机出现「MQTT 连接成功」 |
| 订阅 / 收发 | 默认主题可改 | 发送后本端或对端能收到 |
| 断开 | `client.end()` | 状态变为已断开 |
| 日志 | 时间戳滚动条 | 可见 connect / error / SOCK 线索 |

## 五、用户体验与设计规范

可执行视觉规范见根目录 `DESIGN.md`。

## 六、技术与数据要求

- 栈：uni-app Vue3 + HBuilderX App
- 依赖：**mqtt@3.0.0**（详见 `TECH_DESIGN.md`）
- 协议：`wxs://` Broker URL

## 七、边界与限制

- 账号密码写在页面内，仅联调；外部分发前请自行脱敏
- 小程序 / H5 非本仓库验收重点

## 八、非功能性需求

- 可在 HBuilderX 一键跑到 iOS 自定义基座
- 文档必须写清 mqtt 版本与兼容补丁，便于复制到其他工程
